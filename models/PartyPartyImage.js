
const mongoose = require('mongoose');
const Schema = mongoose.Schema;
const PartyPartyImageSchema = new Schema({
data:String , 
name:String 
});

module.exports = { PartyPartyImageSchema };

